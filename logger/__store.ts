// import { Logger } from "./logger";

// export function logStore(
//   log: Logger,
//   prefix: string,
//   object: Record<string, { subscribe: Function }>
// ) {
//   Object.entries(object).forEach(([name, store]) => {
//     store.subscribe((value) => log(`${prefix} ${name}`, value));
//   });
// }
